<section class="hero-section">
    <div class="logo">
        <a href="{{@site.url}}">
            <img src="{{@site.logo}}" alt="{{@site.title}}">
        </a>
    </div>

    <h1>{{title}}</h1>
</section>
